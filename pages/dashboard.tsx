import { dashboardPage } from '@application';

const { Dashboard, getServerSideProps } = dashboardPage;

// eslint-disable-next-line no-restricted-exports
export { Dashboard as default, getServerSideProps };
